#ifndef TWEET_HPP
#define TWEET_HPP

#include <vector>
#include <string>

class Tweet {
public:

private:
    int tweet_id;
    int profile_id;
    std::vector<int> tags_id;
    std::string text;
    std::string date;
    std::string 
    bool is_visible;
};

#endif
